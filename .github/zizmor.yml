# wepub-release.yml is autogenerated by cargo-dist; ignore the rules it consistently violates.
rules:
  excessive-permissions:
    ignore:
      - wepub-release.yml
  template-injection:
    ignore:
      - wepub-release.yml
  cache-poisoning:
    ignore:
      - wepub-release.yml
  unpinned-images:
    ignore:
      - wepub-release.yml
  use-trusted-publishing:
    ignore:
      - wepub-release.yml
  secrets-inherit:
    ignore:
      - wepub-release.yml
